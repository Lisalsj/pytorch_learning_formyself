from torch.utils.data import Dataset
import cv2
import os


class MyData(Dataset):

    def __init__(self): ##初始化类，一般为整个class类增添全局变量

    def __getitem__(self, index):
        return super().__getitem__(index)
    int a=2